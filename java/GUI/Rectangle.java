package GUI;

import javafx.scene.paint.Color;

public class Rectangle {
    private Rectangle blockRectangle;
    public Rectangle(int dimSquare, int dimSquare1, Color blockColor) {
        this.blockRectangle = new Rectangle(dimSquare, dimSquare1, blockColor);
    }

}
